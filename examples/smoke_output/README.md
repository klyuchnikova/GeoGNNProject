# Synthetic smoke output

These files prove that the entire pipeline executes and that all requested metrics and visualizations are generated. They are produced from `scripts/make_synthetic_data.py` and **must not be reported as Gowalla research results**.

A real Gowalla run should replace them with files in `artifacts/` produced by `configs/gowalla_auto.yaml`.
